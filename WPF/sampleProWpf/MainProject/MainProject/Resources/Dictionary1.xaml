<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="ButtonTemplate1" TargetType="{x:Type Button}">
        <Border x:Name="controlTemplateBorder" BorderBrush="Red" BorderThickness="2" CornerRadius="2" Background="LightBlue" TextBlock.Foreground="Black" 
                                HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                Width="{TemplateBinding Width}" Height="{TemplateBinding Height}"
                                >
            <Grid>
                <ContentPresenter RecognizesAccessKey="True" Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" x:Name="contentPresenter">
                </ContentPresenter>
            </Grid>

        </Border>
    </ControlTemplate>
</ResourceDictionary>